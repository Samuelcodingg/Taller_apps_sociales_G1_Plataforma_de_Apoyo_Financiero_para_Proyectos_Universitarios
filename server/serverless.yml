service: backend-api
frameworkVersion: ~4.34.0

provider: 
  name: aws
  runtime: nodejs20.x
  region: us-east-2
  environment:
    DATABASE_URL: ${env:DATABASE_URL}

functions:
  api:
    handler: dist/lambda.handler
    events: 
      - httpApi: '*'

plugins:
  - serverless-offline